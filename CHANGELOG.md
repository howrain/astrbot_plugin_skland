# Changelog

## v2.0.2 - 2026-04-09

### Changed
- 增加更多 AstrBot 运行日志，补充登录凭证回退、游戏信息查询、便签卡片、公招持有标记、抽卡记录与抽卡分析等关键链路的错误上下文。

### Fixed
- 修复已存 `cred` 失效时会导致所有依赖登录信息的功能整体报错的问题。
- 登录查询链路改为：优先使用已存 `cred`，失败后自动回退为 `token` 刷新凭证再重试。

## v2.0.1 - 2026-04-09

### Changed
- 新增 `render_cache_ttl_seconds` 配置项，可在 AstrBot 配置页调整渲染缓存保留时长，默认保留最近 1 小时。
- README 补充配置说明，并将预览图改为更适合 AstrBot WebUI 的绝对地址展示方式。
- 预览图生成脚本会自动压缩输出尺寸，降低文档展示时的体积和加载压力。

### Fixed
- 修复扫码登录后部分明日方舟查询命令返回“用户未登录”的问题，查询链路会优先复用已存储的 `cred`。
- 优化 `skdscan` 的二维码撤回逻辑，对齐 `endfield` 的成功 / 过期 / 拒绝 / 失败处理流程。
- 改进二维码消息发送与撤回日志，提升 `aiocqhttp` 场景下的撤回稳定性。
- 将 `resources/render_cache/` 加入 `.gitignore`，避免运行时缓存被误纳入版本控制。

## v2.0.0 - 2026-04-09

### Added
- 新增官方森空岛扫码登录 `skdscan`，并保留手动 `token` 登录。
- 新增 `skdhelp`、`skdusers` 等账号与帮助命令，以及中文别名。
- 新增 `arknights` / `明日方舟` / `方舟` 查询入口。
- 新增明日方舟查询能力：便签、理智、剿灭、日常、周常、公告、公招、肉鸽、抽卡记录、抽卡分析。
- 新增一图流材料掉率查询，支持网站截图版与本地渲染版两种输出。
- 新增预览图生成脚本 `scripts/generate_preview.py`，用于 README 展示和回归检查。
- 新增 `docs/preview/` 示例图片与测试报告产物。
- 新增公告详情本地渲染模板与多张查询功能模板。

### Changed
- 将插件结构拆分为 `core/`、`resources/`、`scripts/` 等模块化目录。
- 兼容旧版登录信息与旧存储结构，老用户升级后可继续使用。
- 登录二维码消息在成功、超时或结束后自动撤回，减少账号信息暴露风险。
- 公告来源改为明日方舟官网新闻页，并按最新时间倒序展示。
- 单条公告详情改为本地渲染，展示标题、日期、作者、摘要与正文。
- 抽卡记录与抽卡分析图片样式对齐 Yunzai 风格，补充卡池 UP 头像、颜色分级柱状图与评价统计。
- README 重写为展示型文档，补充命令说明、安装步骤、输出模式和预览图。
- 预览脚本默认对玩家昵称和 UID 做固定脱敏处理。

### Fixed
- 修复一图流本地渲染时材料数量不全的问题，改为全量渲染接口返回内容。
- 修复材料渲染中缺少材料图标、关卡编号显示错误、期望理智精度不足的问题。
- 修复一图流截图范围过大问题，裁切为核心内容区。
- 修复肉鸽数据字段读取错误导致全部显示为 `0` 的问题。
- 修复公告列表可能显示旧公告的问题。
